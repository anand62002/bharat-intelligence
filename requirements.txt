yfinance>=0.2.40
requests>=2.31.0
feedparser>=6.0.11
beautifulsoup4>=4.12.3
lxml>=5.2.0
supabase==2.10.0
python-dotenv>=1.0.0
openai>=1.0.0
anthropic>=0.25.0
langgraph>=0.2.0
apscheduler>=3.10.0
# ── API server ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
PyGithub>=2.3.0
openpyxl>=3.1.0        # DB-10: screener.in Excel export fallback for get_screener_history
# ── Options data — ICICI Breeze Connect (P1-B) ────────────────────────────────
# Optional: only needed when BREEZE_API_KEY / BREEZE_SESSION_TOKEN are configured.
# Install manually: pip install breeze-connect pyotp
breeze-connect>=1.0.59
pyotp>=2.9.0
# ── P6-D-6: Hindi RSS translation (no API key needed) ─────────────────────────
deep-translator>=1.11.0
# ── P6-D-5: spaCy NER entity resolution (optional — graceful fallback if absent)
# spacy>=3.7.0  ← kept commented: Railway memory budget. Install manually if needed.
# Run after install: python -m spacy download en_core_web_sm
# ── CI / testing ───────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-json-report>=1.5.0
