[build]
builder = "nixpacks"

[deploy]
# startCommand intentionally omitted — set per-service in Railway dashboard
# or read from Procfile (web: uvicorn ..., worker: python worker.py)
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
