{
  "_comment": "Vercel project root must be set to 'dashboard/' in Vercel UI (Settings → General → Root Directory).",
  "_comment2": "This root file is intentionally minimal — all build config lives in dashboard/vercel.json."
}
